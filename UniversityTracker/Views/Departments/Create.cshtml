@{
  Layout = "_Layout";
}

@model UniversityTracker.Models.Department
<h1>Add a new Department</h1>
@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)
    <input type="submit" value="Add a new department" class="btn btn-info" />
}